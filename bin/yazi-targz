#!/bin/bash

archive="archive.tar.gz"

first="$1"
base_dir="$(dirname "$first")"

shift 0

files=()

for f in "$@"; do
    files+=("$(basename "$f")")
done

cd "$base_dir" || exit 1

tar -czf "$archive" "${files[@]}"
