[project]
name = "todo-src"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "zwiadowca987", email = "mat.szczerek@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "bs4>=0.0.2",
    "requests>=2.34.2",
    "textual>=8.2.8",
]

[project.scripts]
todo-src = "todo_src:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
